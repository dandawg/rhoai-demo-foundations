apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kueue-cluster-default
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/name: kueue-cluster-default
    app.kubernetes.io/part-of: rhoai-demo-foundations
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: rhoai-distributed
  source:
    repoURL: https://github.com/dandawg/rhoai-distributed.git
    targetRevision: main
    path: platform/kueue/cluster
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 10m
